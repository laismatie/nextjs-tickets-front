export type EventModel = {
  id: string;
  name: string;
  organization: string;
  date: string;
  price: number;
  rating: string;
  image_url: string;
  location: string;
};

export type SpotModel = {
  name: string;
};